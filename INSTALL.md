# 📱 インストール手順

このレッスン記録アプリを各デバイスにインストールする方法を説明します。

## 方法1: GitHub Pagesでホスティング（推奨）

### ステップ1: GitHubでの設定

1. GitHubのリポジトリページにアクセス: `https://github.com/syuua2013/index.html`
2. 画面右上の **Settings**（設定）をクリック
3. 左側のメニューから **Pages** を選択
4. **Source** セクションで:
   - Branch: `claude/music-lesson-app-EahQN` を選択
   - Folder: `/ (root)` を選択
   - **Save** をクリック

5. 数分後、ページ上部に公開URLが表示されます:
   ```
   https://syuua2013.github.io/index.html/
   ```

### ステップ2: 各デバイスでアクセス

#### iPhone / iPad

1. **Safari**で公開URLを開く
   - `https://syuua2013.github.io/index.html/`

2. 画面下部の **共有ボタン**（□に↑のアイコン）をタップ

3. **ホーム画面に追加** を選択

4. アプリ名を確認して **追加** をタップ

5. ✅ ホーム画面にアプリアイコンが追加されます！

**重要**:
- 必ず **Safari** を使用してください（ChromeやFirefoxではホーム画面に追加できません）
- PWA（Progressive Web App）として動作し、ネイティブアプリのように使えます

#### Android

1. **Chrome**で公開URLを開く
   - `https://syuua2013.github.io/index.html/`

2. 画面右上の **メニュー**（⋮）をタップ

3. **ホーム画面に追加** または **アプリをインストール** を選択

4. **インストール** をタップ

5. ✅ ホーム画面にアプリアイコンが追加されます！

#### PC（Windows/Mac/Linux）

1. **Chrome** または **Edge**で公開URLを開く
   - `https://syuua2013.github.io/index.html/`

2. アドレスバー右側に **インストール**（+または⊕）アイコンが表示される

3. アイコンをクリックして **インストール** を選択

4. ✅ デスクトップにアプリとして追加されます！

または、ブックマークとして保存して使用することもできます。

---

## 方法2: Firebase Hostingでホスティング

Firebaseを既に設定している場合、Firebase Hostingを使うのも良い選択です。

### 前提条件

- Node.jsがインストールされていること
- Firebaseプロジェクトが作成されていること

### 手順

1. **Firebase CLIをインストール**
   ```bash
   npm install -g firebase-tools
   ```

2. **Firebaseにログイン**
   ```bash
   firebase login
   ```

3. **プロジェクトディレクトリで初期化**
   ```bash
   cd /path/to/project
   firebase init hosting
   ```

   以下のように設定:
   - プロジェクトを選択
   - Public directory: `.` (現在のディレクトリ)
   - Single-page app: `Yes`
   - GitHub自動デプロイ: お好みで

4. **デプロイ**
   ```bash
   firebase deploy --only hosting
   ```

5. デプロイ完了後、URLが表示されます:
   ```
   https://your-project-id.web.app
   ```

6. このURLを各デバイスで開き、**方法1のステップ2**と同じ手順でホーム画面に追加

---

## 方法3: ローカルで使用（オフラインのみ）

インターネット接続なしで使いたい場合や、テスト用に使えます。

### iPhone / iPad

1. ファイルアプリを使用してプロジェクトファイルをiCloudに保存
2. `index.html`をタップして開く
3. **※ 制限**: ローカルファイルでは一部の機能が制限されます

### PC

1. プロジェクトフォルダをダウンロード
2. `index.html`をダブルクリックして開く
3. **※ 制限**: ローカルファイルではFirebase同期は使えません

---

## マルチデバイス同期の設定

各デバイスでアプリをインストールした後:

1. **最初のデバイス**で:
   - アプリを開く
   - 「ログイン」をクリック
   - 「アカウントを作成」を選択
   - メールアドレスとパスワードを入力
   - アカウント作成

2. **他のデバイス**で:
   - アプリを開く
   - 「ログイン」をクリック
   - 同じメールアドレスとパスワードでログイン

3. ✅ すべてのデバイスでデータが自動同期されます！

---

## トラブルシューティング

### アプリが表示されない

- URLが正しいか確認
- ブラウザのキャッシュをクリア
- プライベートブラウジングモードではないか確認

### ホーム画面に追加できない

- **iPhone/iPad**: Safariを使用しているか確認
- **Android**: Chromeを使用しているか確認
- URLが `https://` で始まっているか確認（GitHub PagesやFirebase Hostingは自動的にHTTPS）

### 同期が動かない

1. Firebase設定が完了しているか確認（`firebase-config.js`）
2. インターネット接続を確認
3. ブラウザのコンソールでエラーを確認

### データが消えた

- ローカルデータは各デバイスのIndexedDBに保存されています
- ブラウザのデータをクリアすると消えます
- **対策**: 定期的にエクスポート機能でバックアップ、またはFirebase同期を有効化

---

## おすすめの使い方

1. **GitHub Pages + Firebase同期** を使用（無料）
2. 全デバイスで同じアカウントでログイン
3. 定期的にエクスポート機能でバックアップを取る
4. オフライン時も自動的にローカルに保存され、オンライン時に同期

これで、iPhone、iPad、PCのどこからでもレッスン記録にアクセスできます！ 🎉
